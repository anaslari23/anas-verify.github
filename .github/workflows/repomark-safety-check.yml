name: RepoMark Safety Check

on:
  workflow_call:
    inputs:
      action:
        description: "Action to perform"
        required: true
        type: string

jobs:
  repomark:
    name: RepoMark verify
    runs-on: ubuntu-latest
    steps:
      - name: Verify Provenance
        run: echo "Running strict verification..."
